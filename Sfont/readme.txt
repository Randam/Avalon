This directory contains two soundfonts.
The soundfonts can be used on a soundblaster compatible, wavetable soundcard.
E.G.: AWE 32, SB 32, AWE 64 etc.

There are two different versions: one for 2 MB RAM and one for 8 MB RAM.

You can use them by loading them into the AWE Control Panel.
